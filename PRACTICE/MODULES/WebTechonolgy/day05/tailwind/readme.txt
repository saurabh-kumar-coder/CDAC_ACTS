tailwind cdn : 
<script src="https://cdn.tailwindcss.com"></script>

to run tailwind in prod
commands :
    1. install node before
    2. npm install tailwindcss postcss autoprefixer
    3. npm install vite
    4. npx tailwindcss init -p 

text-blue-100 --> light blue
text-blue-900 --> dark blue


bg-<color>-<color_weight> --> background color
rounded-lg --> border-rounded
min-h-6 / min-h-[50px] --> giving height
col-span-3 --> take 3 column space
row-span-2 --> take 2 row space

grid --> class to make grid type 
grid-cols-3 --> 3 columns grid
gap-2 --> gap between items (grid items)

grid-flow-row-dense --> it will not leave space in between if the col-span is more