server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saurabh-kumar-coder/microservices-config
          clone-on-start: true