server.port: 8081
spring:
  application:
    name: SPRING-JWT
  datasource:
    url: jdbc:mysql://localhost:3306/spring-jwt
    username: root
    password: tiger
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect